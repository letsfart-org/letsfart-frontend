import Page from '@/components/launch/RPage';
import CreateToken from "@/components/launch/create";

export default function LaunchPage () {
  return (
    <Page>
      <CreateToken />
    </Page>
  )
}
